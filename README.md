# A Simple script that auto select a champion that you pre-select here

### Notes: Before everything make sure you have python3.11 installed on your machine, after it run `pip install -r requirements.txt`
### Usage: Run "./agent select.pyw "agentname"" and let the script auto-select for you.
