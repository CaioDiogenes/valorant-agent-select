# A Simple script that auto select a champion that you pre-select here

Notes: Before everything make sure you have python3.11 installed on your machine and after it run 
`pip install -r requirements.txt`

Usage: Run 
`./agent select.pyw "agentname"`
Enjoy
